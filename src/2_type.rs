fn main() {
    // Primitive types: int, char,
    let one = 1;

    // Type inference
    let two = 2; // rust will decide the type if it can. so writing type is optional

    println!("Hello, Azza1!");
}
